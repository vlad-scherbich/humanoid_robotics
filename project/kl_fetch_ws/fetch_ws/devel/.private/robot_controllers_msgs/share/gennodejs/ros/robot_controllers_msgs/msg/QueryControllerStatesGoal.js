// Auto-generated. Do not edit!

// (in-package robot_controllers_msgs.msg)


"use strict";

const _serializer = _ros_msg_utils.Serialize;
const _arraySerializer = _serializer.Array;
const _deserializer = _ros_msg_utils.Deserialize;
const _arrayDeserializer = _deserializer.Array;
const _finder = _ros_msg_utils.Find;
const _getByteLength = _ros_msg_utils.getByteLength;
let ControllerState = require('./ControllerState.js');

//-----------------------------------------------------------

class QueryControllerStatesGoal {
  constructor(initObj={}) {
    if (initObj === null) {
      // initObj === null is a special case for deserialization where we don't initialize fields
      this.updates = null;
    }
    else {
      if (initObj.hasOwnProperty('updates')) {
        this.updates = initObj.updates
      }
      else {
        this.updates = [];
      }
    }
  }

  static serialize(obj, buffer, bufferOffset) {
    // Serializes a message object of type QueryControllerStatesGoal
    // Serialize message field [updates]
    // Serialize the length for message field [updates]
    bufferOffset = _serializer.uint32(obj.updates.length, buffer, bufferOffset);
    obj.updates.forEach((val) => {
      bufferOffset = ControllerState.serialize(val, buffer, bufferOffset);
    });
    return bufferOffset;
  }

  static deserialize(buffer, bufferOffset=[0]) {
    //deserializes a message object of type QueryControllerStatesGoal
    let len;
    let data = new QueryControllerStatesGoal(null);
    // Deserialize message field [updates]
    // Deserialize array length for message field [updates]
    len = _deserializer.uint32(buffer, bufferOffset);
    data.updates = new Array(len);
    for (let i = 0; i < len; ++i) {
      data.updates[i] = ControllerState.deserialize(buffer, bufferOffset)
    }
    return data;
  }

  static getMessageSize(object) {
    let length = 0;
    object.updates.forEach((val) => {
      length += ControllerState.getMessageSize(val);
    });
    return length + 4;
  }

  static datatype() {
    // Returns string type for a message object
    return 'robot_controllers_msgs/QueryControllerStatesGoal';
  }

  static md5sum() {
    //Returns md5sum for a message object
    return '6ecbb837d1e8545d81a831a4c1c4bfcc';
  }

  static messageDefinition() {
    // Returns full string definition for message
    return `
    # ====== DO NOT MODIFY! AUTOGENERATED FROM AN ACTION DEFINITION ======
    # This action allows one to query current information about
    # controller state, as well as change the controller state.
    
    # Action goal:
    # Requested changes to controller state. Each state must
    # have at least the controller name and new state. If the
    # controller type is non-empty, then it will be used to
    # make sure that the controller type matches. 
    ControllerState[] updates
    
    ================================================================================
    MSG: robot_controllers_msgs/ControllerState
    # Valid states:
    
    uint8 STOPPED = 0
    uint8 RUNNING = 1
    uint8 ERROR = 2
    
    string name
    string type
    uint8 state
    
    `;
  }

  static Resolve(msg) {
    // deep-construct a valid message object instance of whatever was passed in
    if (typeof msg !== 'object' || msg === null) {
      msg = {};
    }
    const resolved = new QueryControllerStatesGoal(null);
    if (msg.updates !== undefined) {
      resolved.updates = new Array(msg.updates.length);
      for (let i = 0; i < resolved.updates.length; ++i) {
        resolved.updates[i] = ControllerState.Resolve(msg.updates[i]);
      }
    }
    else {
      resolved.updates = []
    }

    return resolved;
    }
};

module.exports = QueryControllerStatesGoal;
